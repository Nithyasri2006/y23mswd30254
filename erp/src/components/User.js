import React from 'react'

const User = (props) => {
  return (
    <div>
      <h1> "welcome {props.name} logged in as {props.type} in {props.branch}" </h1>
    </div>
  )
}

export default User






